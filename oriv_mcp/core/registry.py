from oriv_mcp.capabilities.prompts.registry import PromptRegistry
from oriv_mcp.capabilities.resources.registry import ResourceRegistry
from oriv_mcp.capabilities.tools.registry import ToolRegistry

tools = ToolRegistry()
resources = ResourceRegistry()
prompts = PromptRegistry()
