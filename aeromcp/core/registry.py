from aeromcp.capabilities.prompts.registry import PromptRegistry
from aeromcp.capabilities.resources.registry import ResourceRegistry
from aeromcp.capabilities.tools.registry import ToolRegistry

tools = ToolRegistry()
resources = ResourceRegistry()
prompts = PromptRegistry()
